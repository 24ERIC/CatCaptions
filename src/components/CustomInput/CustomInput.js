import React from 'react';
import { View, TextInput, StyleSheet } from 'react-native';

const CustomInput = ({}) => {
    return (
        <View>

        </View>
    );
};

export default CustomInput